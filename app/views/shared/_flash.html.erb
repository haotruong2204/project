<% flash.each do |type, message| %>
  <div class="alert <%= flash_class type.to_sym %> flash-notice">
    <a class="close" data-dismiss="alert" href="#" aria-hidden="true">&times;</a>
    <p class="text-center"><%= message.html_safe %></p>
  </div>
<% end %>
